feat(1.1): Create New Novel - complete implementation with all tests passing

STORY 1.1 COMPLETE - All Acceptance Criteria Met ✅

Helper Layer (100%):
- createNovel(): Creates ~/.netwriter/<novel>/ with manuscript/, wiki/, meta/ subdirectories
- getIndex(): Reads and parses meta/index.json from disk
- Error handling: Structured error envelopes with code, message, suggestion, context
- 6 tests: All PASSING ✅

Electron/IPC Layer (100%):
- preload.js: Securely exposes electronAPI (invoke, on, off) via contextBridge
- ipc-handlers.js: Registers 'helper:index:createNovel' and 'helper:index:get' handlers
- main.js: Simplified lifecycle, calls registerHandlers() on app.whenReady()

Renderer Layer (100%):
- ipc-client.ts: IPC wrapper with domain-specific handlers and error parsing
- NovelSelector.jsx: Modal dialog component for creating new novels
- NovelSelector.css: Complete styling for modal, form, button states
- 5 component tests: All PASSING ✅

Test Summary:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Helper Tests (6/6 PASSING):
  ✓ createNovel - directory structure validation
  ✓ createNovel - empty name error handling
  ✓ createNovel - invalid character validation  
  ✓ createNovel - duplicate novel detection
  ✓ getIndex - read existing index
  ✓ getIndex - handle missing index

Component Tests (5/5 PASSING):
  ✓ NovelSelector renders 'New Novel' button
  ✓ Opens dialog when button clicked
  ✓ Disables Create button when name is empty
  ✓ Enables Create button when name is valid
  ✓ Shows error for invalid novel names

TOTAL: 11/11 Tests Passing ✅

Implementation Notes:
- ESM modules throughout (helper, electron, renderer)
- TDD approach: Tests written first, implementation follows
- Helper functions return structured envelope responses
- Component uses React hooks (useState, useCallback) 
- IPC communication fully typed (TypeScript)
- Error handling: User-friendly messages with code and suggestions
