import { test, expect } from '@playwright/test';

test.describe('E2E Smoke', () => {
  test('runner is wired', async () => {
    // Placeholder to confirm Playwright discovers tests.
    expect(true).toBe(true);
  });
});
