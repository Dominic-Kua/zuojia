Prototype Run Instructions
=========================

From the project root, install dependencies and run the dev environment which starts Vite and Electron.

Install (macOS):

```bash
cd /Users/dominickua/code/ä½å®¶
npm install
npm run dev
```

This starts Vite on http://localhost:5173 and then launches Electron pointing at the dev server. Use Cmd+R in the Electron window to reload.

Notes:
- The prototype is intentionally minimal. It demonstrates the 3/4 manuscript / 1/4 sidebar layout and core components.
- I can convert these into a component library (React + Storybook) or produce Figma assets next.
